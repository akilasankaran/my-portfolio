---
import { HeroContent } from "../motion/HeroContent";
import { site } from "../../data/site";
---

<HeroContent
  client:load
  name={site.name}
  rolePhrases={site.rolePhrases}
  resumeUrl={site.resumeUrl}
/>
